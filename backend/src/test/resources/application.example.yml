spring:
  servlet:
    multipart:
      max-file-size: '5MB'
      max-request-size: '5MB'
      enabled: true
  application:
    name: 'NexoERP'
    security:
      token:
        secret: "123456"
  datasource:
    url: 'jdbc:postgresql://localhost:5432/nexo_erp_tests'
    username: 'postgres'
    password: 'AdminPassword'
  cache:
    type: redis
  data:
    redis:
      host: 'localhost'
      port: 6379
frontend:
  host: 'http://localhost:8081'